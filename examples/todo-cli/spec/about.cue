package todo

// 解説のメタデータ。explain が読んで題名と概要を出す。
about: {
	title:      "TODO CLI(タスクの永続化)"
	purpose:    "状態機械型の原型として、レコードの状態遷移を状態遷移図と不変条件でモデル化する手本を示す"
	scope:      "タスクの追加・開始・完了・再開・削除のコマンドと、それらが変えるタスク集合の保存値"
	exclusions: [
		"タスクのタイトル文字列は値としてはモデル化せず、前提(空でないか)としてだけ扱う",
		"タイムスタンプは値としてはモデル化せず、存在だけを追跡する",
	]
}

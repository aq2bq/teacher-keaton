# AGENTS.md — このリポジトリで作業するAIエージェントへ

この文書は、セッションの文脈を持たないエージェントが `teacher-keaton` を
**正しく理解し、設計判断を壊さずに改善を続けられる**ようにするためのものです。
コードやツールだけを見ても分からない「なぜこうなっているか」をここに残します。

読む順序の推奨: まずこの文書 → `docs/philosophy.md`(なぜ) →
`skills/teacher-keaton/SKILL.md`(手順) → `docs/method.md`(判断基準)。

---

## 1. これは何で、なぜ存在するか(最重要)

**これは「図を生成するツール」ではない。** 図は手段であり、目的は別にある。

出発点の課題: AIエージェントによる開発では、コード生成能力そのものより、
**人間とAIの間の認識のずれ**(概念・用語・責務・状態遷移・不変条件・暗黙の仕様)が
本質的な問題になる。自然言語のドキュメントでは、これらが曖昧になり、
実装との乖離を機械的に検出できない。

このプロジェクトの仮説と目標:

> 実コードを事実源としてリバースエンジニアリングした**形式モデル**を介し、
> 人間が**目的別に**システムの意味を理解するための**セマンティックインターフェース**
> を作る。

つまり「コード → 形式モデル(Specification IR) → 複数の整合したビュー → 人間の理解」
という経路を、Git/CLI/CI の延長のカジュアルな道具立てで成立させること。
**メンタルモデルの構築と照合**が最終的な価値であり、図はそのための照合面にすぎない。

### なぜAIエージェントが自力でこの発想に至りにくいか

エージェントに「このコードを説明して」と頼むと、普通は散文の説明やコメントを返す。
「コードを形式モデルに逆抽出し、検査可能な中間表現として、複数の整合ビューに投影し、
人間のメンタルモデルと照合する」という**メタレベルの発想**は、指示されなければ
まず出てこない。このリポジトリはその発想を具体的な手順とツールに落とし込んだもの。

**だから未来のエージェントは、この文書なしだと「コードから図を作るツール」と
誤認しやすい。** そうではない。形式モデルによる**認識の固定と照合**が本体であり、
図・用語表はその投影にすぎない。改善を行うときも、この本体を見失わないこと。

---

## 2. 経緯(この設計がどこから来たか)

このリポジトリは、別のハンズオンリポジトリ(`cue-quint-specification-ir-hands-on`)での
試行錯誤から生まれた。その経緯を知っていると、設計判断の理由が分かる。

1. **桃太郎で仮説検証**: 誰もが筋を知る昔話を題材に、CUE(構造)とQuint(振る舞い)で
   形式化し、投影・照合まで一周させた。「正解」が共通認識として存在するため、
   モデルの当たり外れを即座に判定できた。
2. **ハードコードの失敗**: 最初のシーケンス図ツールは桃太郎の状態変数と語彙を
   ツール内に焼き込んでおり、**他システムに使えなかった**。この失敗から、
   ドメイン解釈を `projection.cue`(宣言)へ切り出し、ツールを汎用の機械にした。
   **これが本プロジェクトで最も重要な設計判断。**
3. **todo-cliで汎化を確認**: 状態機械(桃太郎はナラティブ)でも同じ手法が成立するかを
   実コード+形式モデルで検証し、成立を確認。この2例が「ナラティブ」と「状態機械」の
   原型(アーキタイプ)として `examples/` に残っている。
4. **4ビューの適応型投影**: 用語表・概念マップ・シーケンス図・状態遷移図を、
   システムの構造に合わせて出し分ける形に収束。
5. **スキル化**: `npx skills add` で配布できるよう、この独立リポジトリに抽出。
6. **`keaton/spec` 既定**: 成果物を `spec/` に置くとRubyのRSpec等と衝突するため、
   専用名前空間を既定にした。
7. **フェーズ0(スコープ合意)の追加**: 対象がLLMまかせだと期待とずれて収束しないため、
   着手前に「理解計画」を合意するフェーズを加えた。

---

## 3. 設計の要点と、その理由(壊してはいけない判断)

改善時にこれらの判断を覆さないこと。それぞれに理由がある。

| 判断 | 理由 |
|---|---|
| **CUE=構造、Quint=振る舞い**の2層 | 「静的に保証できること」と「時間とともに変化するもの」の自然な境界 |
| **CUEが語彙の原本** | 用語・定義を一元化し、全ビューで構造的に一致させるため |
| **ドメイン解釈は `projection.cue`、ツールは汎用** | ツールにドメインを焼き込むと他システムに使えない(経緯2の失敗) |
| **適応型ビュー(すべてを強制しない)** | ナラティブに状態図を、状態機械にシーケンス図を無理に出さない |
| **健全だが完全ではない** | 全部の形式化は破綻する。機械検査する価値のあるものだけ形式化 |
| **成果物は `keaton/spec`** | 各言語の `spec/` 慣習(RSpec等)と衝突しないため |
| **スキル利用時の生成ファイルは実行場所の `keaton/` に収める** | 形式モデル、解説書、一時ファイルが `/tmp`、`./tmp`、`./keaton` に分散すると、照合対象とGitで管理すべき範囲が利用ごとに変わるため。形式モデルは `keaton/spec/`、解説書は `keaton/explanation.md`、検証詳細ログは `keaton/verify.log`、一時ファイルは `keaton/tmp/` とする。`examples/` の明示specパスは開発と回帰専用 |
| **フェーズ0でスコープ合意** | 対象が曖昧なままだと時間とクレジットを浪費し収束しないため |
| **観測事実のみモデル化、意図は散文** | 願望をモデルに足すと照合の基準が壊れるため |
| **コードだけでは決まらない食い違いは `keaton/TODO.md`** | 推測した前提を形式検証で補強せず、根拠と反証を人間の判断へ渡すため |
| **`examples/` は配布しない(開発・回帰専用)** | 具体的な完成例は模倣の锚(アンカー)となって出力の収束を妨げ、行使をトークンヘビーにするため。配布物は `SKILL.md`+`templates/` だけで自己完結させる |
| **図のラベルは語彙から解決する(イベントも概念)** | 図に出る語の自由記述を許すと、同じイベントがビューやセッションごとに別の言い方になり得て、認識のずれが生まれる。「図にはあるがモデルには無い語」というハルシネーションの露出面も消すため、ラベルはイベントのidから語彙の表示名へ解決し、`projection.cue` にラベルは書かない |
| **概念マップは中心概念と近傍深度で絞れる** | 全体図が大きいと利用者の問いに必要な関係を追いにくい。`--focus` は用語または安定idを受け付け、入向き・出向きの両方をたどって `--focus-depth` 辺以内だけを描く。再現性が必要な実行では改名に影響されない安定idを使う。オプション省略時は関係の書き漏れを発見できる従来の全体図を維持する |
| **形式化の範囲は `quintExpected` が正本** | 「健全だが完全ではない」が原則なので、全概念のQuint未使用を警告すると雑音になる(実適用で43中42件が警告)。行動の核だけ `vocabulary.cue` の `quintExpected` に宣言し、その未使用だけを「モデルの穴」として警告する。使用判定は、通常概念の生成文字列定数、測度の `quintVar` に対応する変数宣言、閾値から生成した定数・述語のモデル本体での参照を数え、importしただけでは数えない |
| **観測事実の根拠は概念の `sources` に残す** | 根拠のないモデルはハルシネーションと区別できない。特にコード以外(文書・表)を事実源にする適用で監査性を担保するため、`location` は自由形式(行番号に縛られない)で、用語表に根拠カラムとして出す |
| **境界条件は正例・負例の両方で検証する** | `cue vet` は矛盾しか検出できず、緩すぎる制約(許可すべきでないものを許可する)は通してしまう。「モデルが実際には保証していないことを保証しているように見える」のを防ぐため、specの `positives/`・`negatives/` で「通るべきものが通り、拒むべきものが拒まれる」ことを `tools/vet` が機械的に検証する |
| **`verify` は4→8→12の段階探索で到達深度を記録する** | 検証コストは深度に対して掛け算で爆発しうるため、深い深度の一発勝負は「長時間待って何も得られない」になり得る。浅い深度から始めて段ごとに時間上限を設ければ、どの時点で止まっても「どこまで保証できたか」が残る。標準出力は成否と到達深度の要約に限定し、Quint・Apalacheの詳細は `verify.log` へ常に保存する。`--verbose` は同じ詳細を端末にも表示する。浅い深度でのタイムアウトは、対象の拡散を知らせ、スコープ再合意か抽象化を提案する引き金になる |
| **形式モデルの検証と現場運用の実効性を分ける** | `verify` が確認するのは、形式モデルに置いた前提に対して、結果に記載された探索深度まで不変条件の反証が見つからないこと。教育・記録・遵守が現場で実施されているかは確認しない。「検証成功」が運用の適合証明に見えるのを防ぐため、端末は「反証なし」と到達深度を報告し、解説書は検証対象外の現場運用を明示する |
| **数値の極性は宣言し、比較の向きは導出する** | 数値項目は範囲だけでは意味が決まらず、「値が大きいほど良いか悪いか」という契約(極性)が別にある。名前は向きの根拠にならない(「品質」「満足度」という名前で悪さを測っていることがある)ため、極性を根拠つきでCUEに宣言し、CUEの用例(`#MeasureVerdict`)・Quintの述語(生成)・投影の述語の3経路すべてを極性から導出する。手で `>=` と `<=` を書き分ける箇所を作らなければ、仕様・実装・テスト・図の間で向きが食い違えない。実適用で「名前が肯定的な尺度が実は小さいほど良い」という取り違えが起きたことへの対処 |
| **最終報告は明示的に照合の判定を依頼する** | ビューは照合のための「提案」であり、人間の役目はそれを判定すること。判定を依頼しなければ成果物は完成品として鵜呑みにされ、照合が起きない。フェーズ0で合意した照合基準を出口でも機能させるため、最終報告は判定依頼の列挙・照合基準との突合・反応のしかたの案内を、`TODO.md` の有無にからず必ず含む |

---

## 4. リポジトリ構造

```
teacher-keaton/
├── AGENTS.md / README.md / CHANGELOG.md / LICENSE(MIT)
├── docs/                       # 人間(貢献者)向け: 哲学・手法・規約・ツール
│   ├── philosophy.md           # なぜこの手法か(構想・原則・反面教師)
│   ├── method.md               # 逆抽出→モデル化→投影→照合の判断基準
│   ├── conventions.md          # specの構造・projection.cueの書き方
│   └── tools.md                # ツールリファレンス
├── skills/teacher-keaton/
│   ├── SKILL.md                # エージェントが読む手順書(配布される本体)
│   ├── tools/                  # Bunツール(explain/project/glossary/measures/…+lib)
│   └── templates/              # 新規プロジェクト用spec雛形
└── examples/                   # 2つのアーキタイプの手本
    ├── momotaro/               # ナラティブ → シーケンス図
    └── todo-cli/               # 状態機械 → 状態遷移図(実コード+形式モデル)
```

`skills/teacher-keaton/` が `npx skills add` でインストールされる対象。
`docs/` は継続開発のための人間向けドキュメントで、インストール対象には含まれない。
したがって**エージェントが実行時に読むのは `SKILL.md` と `tools/`** であり、
`docs/` の内容は必要なら `SKILL.md` 側に反映すること。
`examples/` も同様にインストール対象外(手本であり回帰テスト、開発専用)。
**配布物(`SKILL.md`/`templates/`/`tools/`)から、配布外のファイル
(`docs/`・`examples/`)を参照してはならない**。必要な規約は `SKILL.md` へ蒸留する。

---

## 5. やってはいけないこと(アンチパターン)

- **ツールにドメイン知識をハードコードする**。ドメイン解釈は必ず `projection.cue` へ。
  新しい図形式やビューを追加するときも、エンジン(汎用)と解釈(宣言)の分離を保つ。
- **全ビューを強制する**。システムの構造に合うビューだけ出す。
- **全てを形式化しようとする**。価値のある部分に絞り、残りは散文で残す。
- **成果物を `spec/` に置く**。既定は `keaton/spec`。
- **フェーズ0(スコープ合意)を飛ばして形式化に着手する**。
- **意図の推測をモデルに書く**。観測事実のみをモデル化し、意図は散文で残す。
- **検証を通すために語彙や制約を発明する**。コードだけでは決まらない食い違いは、
  根拠と保留範囲を `keaton/TODO.md` に残してユーザーへ判断を求める。
- **`SKILL.md` と `docs/` を乖離させる**。実行時に読まれるのは `SKILL.md`。
  手順や規約を変えたら両方を揃えること。
- **配布物から配布外のファイルを参照する**。スキルは `skills/teacher-keaton/` だけが
  コピーされてインストールされるため、`docs/`・`examples/` への参照はインストール先で
  宙吊りになる。テストも配布先だけで完結しなければならない。

---

## 6. 検証方法(変更したら必ず確認)

リポジトリルートから:

```sh
# ツールの単体テスト
( cd skills/teacher-keaton/tools && bun test )

# examples が正しく投影できるか(回帰)。ツールはcwd非依存・相対/絶対パスどちらでも可
skills/teacher-keaton/tools/explain  examples/momotaro/spec  --test fullStoryTest
skills/teacher-keaton/tools/explain  examples/todo-cli/spec  --test reopenTaskTest
skills/teacher-keaton/tools/explain  examples/todo-cli/spec  --test busyStartRecordedTest
skills/teacher-keaton/tools/check-consistency examples/todo-cli/spec

# 測度(数値尺度)の回帰。fixtureは配布物の中にあり、bun test からも回る
skills/teacher-keaton/tools/vet               skills/teacher-keaton/tools/fixtures/measure-spec
skills/teacher-keaton/tools/check-consistency skills/teacher-keaton/tools/fixtures/measure-spec
skills/teacher-keaton/tools/measures          skills/teacher-keaton/tools/fixtures/measure-spec
skills/teacher-keaton/tools/verify   examples/momotaro/spec  # 不変条件の全件検証(段階探索4→8→12、約20秒)
```

`verify` は深度4→8→12の段階探索で、1段あたり既定60秒。
`examples/todo-cli` に対して行うと、タイムスタンプ文字列のSMT処理で
浅い深度でも時間切れになるため、既定の回帰には含めない。

前提: `cue` / `quint` / `bun` が `PATH` に必要(`brew install cue quint bun`)。
`examples/` は「手本であり回帰テスト」なので、ツールや規約を変えたら必ず通すこと。

---

## 7. 現状と今後の方向(長期β)

これは**長期β**であり、実プロジェクトへの適用を通じて継続的に収束させる。
完成形を前もって決めつけず、実適用から学ぶ姿勢を保つこと。

- 現状: 4ビューの適応型投影(+測度を宣言した場合の測度表)、フェーズ0のスコープ合意、
  2アーキタイプの手本が揃った状態。
- 今後の候補(固定の計画ではなく、実適用に応じて優先づけが変わる):
  - 実際のリポジトリでの初適用(βとしての最大の検証)
  - インストール実証(`npx skills add` が別環境で通るか)
  - CI(ツールテストをGitHub Actionsで回す)
  - 関係性の横断検査(CUEのrelations宣言とQuintの振る舞いで行使される関係のdiff)

### 改善時の心構え

- **本体(認識の固定と照合)を見失わない**。個々のツールや図は手段。
- **実装より文書が先にならないよう注意しつつ、設計判断は必ず文書に残す**。
  このリポジトリの持続性は「なぜこうなっているか」が再現できることにかかっている。
- **過剰設計を避ける**。カジュアルな道具立て(plain text / Git / CLI)という原則から
  外れる重厚な仕組み(専用GUI、独自DSL、オントロジー構築)には振らない。
